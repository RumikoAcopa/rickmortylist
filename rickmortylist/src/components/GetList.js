import React, { component } from 'react';
import Proptypes, { component } from 'proptypes';

GetList.proptypes = {

};

function GetList(props) {

  return (

    <div>

    </div>
  );

};

export default GetList;